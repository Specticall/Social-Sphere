export default function Feeds({
  activeUser,
  userData,
  setActivePage,
  activePage,
}) {
  const props = {
    activeUser,
    userData,
    setActivePage,
    activePage,
  };

  return <div className="page__feeds">FEEDS</div>;
}
